#include <stdio.h>
#include <stdlib.h>
/* more headers goes there */

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int n;
	char cha;

		
	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar("%c\n", cha);
	}
	
	return (0);
}
